# Backend (FastAPI) — origin only; axios uses ${VITE_API_ORIGIN}/api/v1
VITE_API_ORIGIN=http://localhost:8000

# Or set the full API base in one variable (overrides the line above):
# VITE_API_URL=http://localhost:8000/api/v1
